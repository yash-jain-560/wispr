# Copy this file to .env and customize values.
# Then use: ./run start

# option | fn
TRIGGER_KEY=option

# cloud | local
AI_MODE=cloud

# Required for cloud mode
GEMINI_API_KEY=replace_me
GEMINI_MODEL=gemini-2.5-flash
# Model used only for audio->text transcription
GEMINI_TRANSCRIBE_MODEL=gemini-2.5-flash
# Optional fallback model if primary transcription model is rate-limited
GEMINI_TRANSCRIBE_FALLBACK_MODEL=gemini-2.0-flash
GEMINI_URL=https://generativelanguage.googleapis.com
# 1 = faster (single Gemini call does transcribe+cleanup+translate), 0 = two-step pipeline
CLOUD_SINGLE_PASS=1
# Keep a small tail after key release so last syllables are not cut off
RECORDING_TAIL_MS=220

# Optional output language
# TARGET_LANGUAGE=English

# Logging controls
# debug | info | warning | error
LOG_LEVEL=info

# 0 (default) masks secrets in logs, 1 prints full values
LOG_SHOW_SECRETS=0

# Local mode settings
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen2.5:3b-instruct

# AI-only local speech-to-text (whisper.cpp)
# LOCAL_STT_MODEL_PATH=/absolute/path/to/ggml-base.en.bin
LOCAL_STT_COMMAND=whisper-cli
LOCAL_STT_MODEL_PATH=
# Optional language code (unset = auto-detect), e.g. en, hi, es
# LOCAL_STT_LANGUAGE=
